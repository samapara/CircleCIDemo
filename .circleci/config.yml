version: 2.1
jobs:
        build:
                docker:
                        - image: cirlceci/node
                steps:
                        - checkout
                        - run: echo "Hello, World!"
